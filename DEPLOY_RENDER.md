# 外部サーバー(Render.com)へのデプロイ手順

社内Wi-Fiのファイアウォール制限を回避するため、無料の外部ホスティングサービス
「Render.com」にアプリを置く方法です。クレジットカード登録不要、完全無料の範囲で動きます。

これなら、見る側は普通にインターネット経由でURLにアクセスするだけなので、
ESETのファイアウォール(ローカル通信のブロック)の影響を受けません。

---

## 事前準備

- GitHubアカウント(無料・未保有なら https://github.com で作成)
- Renderアカウント(無料・ https://render.com でGitHub連携してサインアップ)

---

## 手順

### 1. GitHubにコードを置く

1. GitHubで新しいリポジトリを作成(例: `parking-app`、Public/Privateどちらでも可)
2. `parking_app` フォルダの中身(`server.py`, `index.html`, `README.md`)をアップロード
   - GitHubの画面で "uploading an existing file" からドラッグ&ドロップでもOK
   - `parking_data.json` がもしあれば、アップロードしなくてOK(自動生成されます)

### 2. Renderでサービスを作成

1. https://render.com にログイン
2. 右上の "New +" → "Web Service" を選択
3. 先ほどのGitHubリポジトリを選んで連携
4. 設定項目を入力:
   - **Name**: 好きな名前(例: `parking-map`)
   - **Region**: Singapore(日本から一番近い)
   - **Branch**: main
   - **Runtime**: Python 3
   - **Build Command**: 空欄のままでOK(追加ライブラリ不要のため)
   - **Start Command**: `python server.py`
   - **Instance Type**: Free を選択
5. "Create Web Service" をクリック

数分待つとデプロイが完了し、`https://parking-map-xxxx.onrender.com` のようなURLが発行されます。
このURLを社内で共有すれば、インターネットに繋がっている端末ならどこからでもアクセスできます。

---

## 注意点(無料プランの制限)

- **15分アクセスがないとスリープします。** 再度アクセスがあると自動的に起き上がりますが、
  最初の1回だけ表示まで30〜50秒ほどかかります。「重い」のではなく「起こしている」状態です。
- **再デプロイやスリープからの復帰時に、保存していた駐車状況がリセットされる場合があります。**
  Renderの無料プランはディスクが一時的なものなので、長期保存が必要な場合は将来的に
  Render の無料DB(PostgreSQL)などに切り替えるのがおすすめです(今は構成がシンプルなので
  まずはこのままで様子を見るのが良いと思います)。
- **誰でもURLを知っていればアクセスできます。** 社外に漏れて困る情報でなければ問題ありませんが、
  気になる場合は後述の簡易パスワード保護を検討してください。

---

## (任意)簡易パスワード保護を付けたい場合

URLを知っていれば誰でも編集できてしまうのが気になる場合は教えてください。
簡単なパスワード入力画面を追加することもできます。
